/** @format */

const Spinner = () => {
  return (
    <div className="relative flex">
      <div className="absolute top-[40%] left-[45%] animate-ping w-16 h-16 m-8 rounded-full bg-sky-600"></div>
    </div>
  );
};

export default Spinner;
